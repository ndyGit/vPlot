define(function(require) {
	var template = (function() {

		// TEST TREE
		var Tree = (function() {
			this.name = '';
			this.config = '';
			this.childs = [];
		});

		var template = new Tree();
		template.name = '2d';
		template.config = {
	
		}

		var line_chart = new Tree();
		line_chart.name = 'line_chart';
		line_chart.config = {
		};

		var dataset1 = new Tree();
		dataset1.name = 'dataset2d';
		dataset1.config = {
			path : 'testdata'
		}

		line_chart.childs.push(dataset1);
		template.childs.push(line_chart);

		return template;
	})();
	return template;
})